#pragma once

#include "model/raw_image.h"

using namespace std;

namespace gimi {

  class ImageProcessor {

  public:
    // static RawImageGrid cut_image_into_tiles(const RawImage &image, uint32_t rows, uint32_t columns);
  };

} // namespace gimi