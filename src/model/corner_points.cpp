#include "corner_points.h"

// Constructors
CornerPoints::CornerPoints(const Point &top_left, const Point &top_right, const Point &bottom_left,
                           const Point &bottom_right)
    : top_left(top_left), top_right(top_right), bottom_left(bottom_left), bottom_right(bottom_right) {
  //
}
