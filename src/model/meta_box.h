#pragma once
#include "infe_item.h"
#include <vector>

using namespace std;

namespace gimi {

  class MetaBox {
  private:
    vector<InfeItem> m_items;
    // references
    // groups
  };

} // namespace gimi